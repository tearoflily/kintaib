    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <% if logged_in? %>
          <%= link_to "勤怠システム", user_works_path(current_user), id: "logo" %>
        <% else %>
          <%= link_to "勤怠システム", root_path, id: "logo" %>
        <% end %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if logged_in? %>
              <li><%= link_to "トップへ", user_works_path(current_user) %></li>
            <% else %>
              <li><%= link_to "トップへ", root_path %></li>
            <% end %>
             
            <% if logged_in? && current_user.admin? %>
              <li><%= link_to "ユーザー一覧", users_path %></li>
            <% end %>
        <% if logged_in? && current_user.admin? %> 
          <li><%= link_to "基本情報の修正", user_works_path(current_user) %></li>
        <% end %>    
        <% if logged_in? %>
        
         <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              アカウント <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "ユーザー詳細", current_user %></li>
              <li><%= link_to "アカウント情報の更新", edit_user_path(current_user.id) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "ログアウト", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
          <% else %>
          <li><%= link_to "ログイン", login_path %></li>
          <% end %>
          
          </ul>
        </nav>
      </div>
    </header>